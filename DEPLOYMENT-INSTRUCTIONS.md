# 🚀 دليل النشر على Cloudflare Pages

## ✅ التعديلات المكتملة:

### 1️⃣ **functions/api/data.js**
- ✅ تم تعديل GET request لقراءة `poultry-data` من KV
- ✅ تم تعديل POST request لحفظ على `poultry-data` في KV
- ✅ إضافة CORS headers للسماح بالوصول
- ✅ إنشاء نسخ احتياطية تلقائية (تنتهي بعد 7 أيام)

### 2️⃣ **admin/dashboard-new.js**
- ✅ تم تعديل `loadFullData()` لتحميل البيانات من `/api/data` أولاً
- ✅ Fallback إلى `full-data.json` المحلي إذا فشل KV
- ✅ تم تعديل `saveAllChanges()` لاستخدام `/api/data` بدلاً من PHP
- ✅ رسائل خطأ واضحة للمستخدم

### 3️⃣ **auto-update.js**
- ✅ جاهز بالفعل - يقرأ من `/api/data`
- ✅ Fallback للبيانات الثابتة إذا فشل الاتصال

### 4️⃣ **wrangler.toml**
- ✅ تم إنشاء الملف مع:
  - اسم المشروع: `poultry-prices`
  - KV namespace ID: `82529e7cbc5347f9b5d7ea407f26dba9`
  - Build output directory: `.`

### 5️⃣ **.gitignore**
- ✅ تم تحديث لاستبعاد ملفات Node و Wrangler

---

## 📋 خطوات النشر (بعد تثبيت Node.js):

### **الخطوة 1: تثبيت Node.js**
1. حمّل من: https://nodejs.org/
2. ثبّت النسخة **LTS** (الموصى بها)
3. أعد فتح PowerShell

### **الخطوة 2: تثبيت Wrangler**
```powershell
npm install -g wrangler
```

### **الخطوة 3: تسجيل الدخول لـ Cloudflare**
```powershell
wrangler login
```
سيفتح متصفح - سجل دخول بحساب Cloudflare

### **الخطوة 4: رفع البيانات الأولية على KV**
```powershell
wrangler kv:key put --namespace-id=82529e7cbc5347f9b5d7ea407f26dba9 "poultry-data" "$(Get-Content 'admin\full-data.json' -Raw)"
```

### **الخطوة 5: نشر المشروع على Cloudflare Pages**
```powershell
wrangler pages deploy . --project-name=poultry-prices
```

---

## 🔗 ربط الدومين:

بعد النشر، اذهب إلى:
1. Cloudflare Dashboard → Pages
2. اختر مشروع `poultry-prices`
3. Custom Domains → Add domain
4. أدخل `dwajn.net`

---

## ✅ النتيجة النهائية:

- ✅ الموقع يعمل على Cloudflare Pages
- ✅ لوحة التحكم تحفظ على Cloudflare KV مباشرة
- ✅ التحديثات فورية على الموقع
- ✅ لا حاجة لـ GitHub في كل مرة
- ✅ سرعة عالية عبر شبكة Cloudflare العالمية

---

## 📝 ملاحظات:

- كل التعديلات محفوظة على Git (commit b9d0d98)
- الملفات جاهزة 100% للنشر
- فقط تحتاج تثبيت Node.js لمرة واحدة
- بعدها كل شيء من Command Line مباشرة!

---

**الآن: ثبّت Node.js وقولي لنكمل! 🚀**
